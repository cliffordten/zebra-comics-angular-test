import { SlideHorizontalDirective } from './slide-horizontal.directive';

describe('SlideHorizontalDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideHorizontalDirective();
    expect(directive).toBeTruthy();
  });
});
